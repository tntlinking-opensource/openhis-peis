<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.system.dao.SysVersionBranch1Mapper">

    <resultMap type="com.center.medical.system.bean.model.SysVersionBranch1" id="SysVersionBranchMap">
        <id property="id" column="id"/>
        <result property="branchId" column="branch_id"/>
        <result property="versionId" column="version_id"/>
        <result property="deleted" column="deleted"/>
        <result property="status" column="status"/>
        <result property="finishTime" column="finish_time"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSysVersionBranchVo">
        select id,
               branch_id,
               version_id,
               deleted,
               status,
               finish_time,
               createdate,
               modifydate
        from sys_version_branch
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SysVersionBranchMap">
        <include refid="selectSysVersionBranchVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SysVersionBranchMap">
        <include refid="selectSysVersionBranchVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

