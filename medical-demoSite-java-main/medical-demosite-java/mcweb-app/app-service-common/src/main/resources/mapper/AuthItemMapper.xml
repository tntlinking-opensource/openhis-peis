<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.app.dao.AuthItemMapper">

    <resultMap type="com.center.medical.app.bean.model.AuthItem" id="AuthItemMap">
        <id property="id" column="id"/>
        <result property="authFlag" column="auth_flag"/>
        <result property="authName" column="auth_name"/>
        <result property="setting" column="setting"/>
        <result property="remark" column="remark"/>
        <result property="expiryDate" column="expiry_date"/>
        <result property="status" column="status"/>
        <result property="creater" column="creater"/>
        <result property="createTime" column="create_time"/>
        <result property="updater" column="updater"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectAuthItemVo">
        select id,
               auth_flag,
               auth_name,
               setting,
               remark,
               expiry_date,
               status,
               creater,
               create_time,
               updater,
               update_time
        from md_auth_item
    </sql>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="AuthItemMap">
        <include refid="selectAuthItemVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

