<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.appadmin.dao.AppointmentMapper">

    <resultMap type="com.center.medical.appadmin.bean.model.Appointment" id="AppointmentMap">
        <id property="id" column="id"/>
        <result property="reservationNo" column="reservation_no"/>
        <result property="branchId" column="branch_id"/>
        <result property="fUsecodehiden" column="f_usecodehiden"/>
        <result property="levelId" column="level_id"/>
        <result property="reservationDate" column="reservation_date"/>
        <result property="timeId" column="time_id"/>
        <result property="userId" column="user_id"/>
        <result property="comboId" column="combo_id"/>
        <result property="realName" column="real_name"/>
        <result property="idcard" column="idcard"/>
        <result property="sex" column="sex"/>
        <result property="mobile" column="mobile"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="failReasion" column="fail_reasion"/>
        <result property="createdate" column="createdate"/>
        <result property="creator" column="creator"/>
        <result property="modifydate" column="modifydate"/>
        <result property="modifier" column="modifier"/>
        <result property="branchName" column="branch_name"/>
        <result property="levelName" column="level_name"/>
        <result property="comboName" column="combo_name"/>
        <result property="isDelete" column="is_delete"/>
        <result property="numorgresv" column="numorgresv"/>
        <result property="patientcode" column="patientcode"/>
        <result property="idOrg" column="id_org"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectAppointmentVo">
        select id,
               reservation_no,
               branch_id,
               f_usecodehiden,
               level_id,
               reservation_date,
               time_id,
               user_id,
               combo_id,
               real_name,
               sex,
               idcard,
               mobile,
               remark,
               `status`,
               fail_reasion,
               createdate,
               creator,
               modifydate,
               modifier,
               branch_name,
               level_name,
               combo_name,
               is_delete,
               numorgresv,
               patientcode,
               id_org
        from md_appointment
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="AppointmentMap">
        <include refid="selectAppointmentVo"/>
        <where>
            is_delete = 0
            AND user_id = #{param.userId}
            <if test="param.statusList!=null ">
                AND `status` IN
                <foreach collection="param.statusList" item="status" open="(" close=")" separator=",">
                    #{status}
                </foreach>
            </if>
        </where>
        ORDER BY reservation_date DESC
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="AppointmentMap">
        <include refid="selectAppointmentVo"/>
        <where>
            is_delete = 0
            AND id = #{id}
        </where>
    </select>

</mapper>

