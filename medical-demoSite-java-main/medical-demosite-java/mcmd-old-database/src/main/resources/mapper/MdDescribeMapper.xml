<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.olddata.dao.MdDescribeMapper">

    <resultMap type="com.center.medical.olddata.bean.model.MdDescribe" id="MdDescribeMap">
        <id property="id" column="id"/>
        <result property="patientcode" column="patientcode"/>
        <result property="itemId" column="item_id"/>
        <result property="itemName" column="item_name"/>
        <result property="inspectDescribe" column="inspect_describe"/>
        <result property="signList" column="sign_list"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="depId" column="dep_id"/>
        <result property="tjlx" column="tjlx"/>
        <result property="shortCode" column="short_code"/>
        <result property="depDescription" column="dep_description"/>
        <result property="feeId" column="fee_id"/>
        <result property="feeName" column="fee_name"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdDescribeVo">
        select id,
               patientcode,
               item_id,
               item_name,
               inspect_describe,
               sign_list,
               createdate,
               modifydate,
               dep_id,
               tjlx,
               short_code,
               dep_description,
               fee_id,
               fee_name
        from md_describe
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdDescribeMap">
        <include refid="selectMdDescribeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdDescribeMap">
        <include refid="selectMdDescribeVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

