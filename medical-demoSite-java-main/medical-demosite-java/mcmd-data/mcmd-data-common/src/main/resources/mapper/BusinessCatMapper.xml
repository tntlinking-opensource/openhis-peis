<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.data.dao.BusinessCatMapper">

    <resultMap type="com.center.medical.data.bean.model.BusinessCat" id="BusinessCatMap">
        <id property="typeId" column="type_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="typeName" column="type_name"/>
        <result property="inputCode" column="input_code"/>
        <result property="icon" column="icon"/>
        <result property="tip" column="tip"/>
        <result property="seq" column="seq"/>
        <result property="status" column="status"/>
        <result property="grade" column="grade"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectBusinessCatVo">
        select type_id,
               parent_id,
               type_name,
               input_code,
               icon,
               tip,
               seq,
               status,
               grade,
               createdate,
               modifydate
        from md_business_cat
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="BusinessCatMap">
        <include refid="selectBusinessCatVo"/>
        <where>
            status = 1
            <if test="param.inputCode!=null and param.inputCode!=''">
                AND input_code like concat(#{param.inputCode},'%')
            </if>
            <if test="param.typeName!=null and param.typeName!=''">
                AND type_name like concat(#{param.typeName},'%')
            </if>
        </where>
        ORDER BY seq
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="BusinessCatMap">
        <include refid="selectBusinessCatVo"/>
        <where>
            type_id = #{id}
        </where>
    </select>

    <select id="getList" resultType="com.center.medical.data.bean.model.BusinessCat">
        <include refid="selectBusinessCatVo"/>
        <where>
            status = 1
            <if test="param.inputCode!=null and param.inputCode!=''">
                AND input_code like concat(#{param.inputCode},'%')
            </if>
            <if test="param.typeName!=null and param.typeName!=''">
                AND type_name like concat(#{param.typeName},'%')
            </if>
        </where>
    </select>

    <!--导出业务类型-->
    <select id="getExportData" resultType="com.center.medical.data.bean.model.BusinessCat">
        <include refid="selectBusinessCatVo"/>
        <where>
            status = 1
            <if test="param.inputCode!=null and param.inputCode!=''">
                AND input_code like concat(#{param.inputCode},'%')
            </if>
            <if test="param.typeName!=null and param.typeName!=''">
                AND type_name like concat(#{param.typeName},'%')
            </if>
        </where>
    </select>
</mapper>

