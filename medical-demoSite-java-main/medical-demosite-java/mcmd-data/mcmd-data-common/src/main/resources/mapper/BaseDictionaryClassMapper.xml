<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.center.medical.data.dao.BaseDictionaryClassMapper">

        <resultMap type="com.center.medical.data.bean.model.BaseDictionaryClass" id="BaseDictionaryClassMap">
            <id property="id" column="id"/>
            <result property="createdate" column="createdate"/>
            <result property="modifydate" column="modifydate"/>
            <result property="className" column="class_name"/>
            <result property="classCode" column="class_code"/>
            <result property="isMatchable" column="is_matchable"/>
        </resultMap>

        <!-- 数据表字段属性 -->
        <sql id="selectBaseDictionaryClassVo">
            select id, createdate, modifydate, class_name, class_code, is_matchable            from base_dictionary_class
        </sql>

        <!-- 查询列表数据 -->
        <select id="getList" resultMap="BaseDictionaryClassMap">
            <include refid="selectBaseDictionaryClassVo"/>
        </select>

        <!-- 根据主键id获取记录详情 -->
        <select id="getInfoById" resultMap="BaseDictionaryClassMap">
            <include refid="selectBaseDictionaryClassVo"/>
            <where>
                id = #{id}
            </where>
        </select>
        
    </mapper>

