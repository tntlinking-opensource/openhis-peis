<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.data.dao.YblxMapper">

    <resultMap type="com.center.medical.data.bean.model.Yblx" id="YblxMap">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="isDelete" column="is_delete"/>
        <result property="srm" column="srm"/>
        <result property="xh" column="xh"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectYblxVo">
        select id, name, createdate, modifydate, is_delete, srm, xh
        from md_yblx
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="YblxMap">
        <include refid="selectYblxVo"/>
    </select>
    <!-- 根据条件查询列表数据 -->
    <select id="getList" resultMap="YblxMap">
        select id,
        name,
        srm
        from md_yblx
        <where>
            is_delete = 0
            <if test="param.inputCode!=null and param.inputCode!=''">
                AND srm LIKE concat('%',#{param.inputCode},'%')
            </if>
        </where>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="YblxMap">
        <include refid="selectYblxVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

