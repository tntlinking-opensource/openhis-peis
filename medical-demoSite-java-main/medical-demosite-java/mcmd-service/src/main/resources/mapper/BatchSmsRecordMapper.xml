<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.dao.BatchSmsRecordMapper">

    <resultMap type="com.center.medical.bean.model.BatchSmsRecord" id="BatchSmsRecordMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectBatchSmsRecordVo">
        select id, createdate, modifydate, phone, status
        from md_batch_sms_record
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="BatchSmsRecordMap">
        <include refid="selectBatchSmsRecordVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="BatchSmsRecordMap">
        <include refid="selectBatchSmsRecordVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

