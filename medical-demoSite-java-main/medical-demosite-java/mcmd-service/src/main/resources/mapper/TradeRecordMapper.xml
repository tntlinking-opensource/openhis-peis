<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.dao.TradeRecordMapper">

    <resultMap type="com.center.medical.bean.model.TradeRecord" id="TradeRecordMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="patientcode" column="patientcode"/>
        <result property="outTradeNo" column="out_trade_no"/>
        <result property="money" column="money"/>
        <result property="note" column="note"/>
        <result property="tradeType" column="trade_type"/>
        <result property="tempItemIds" column="temp_item_ids"/>
        <result property="status" column="status"/>
        <result property="registerId" column="register_id"/>
        <result property="orderType" column="order_type"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectTradeRecordVo">
        select id,
               createdate,
               modifydate,
               patientcode,
               out_trade_no,
               money,
               note,
               trade_type,
               temp_item_ids,
               status,
               register_id,
               order_type
        from md_trade_record
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="TradeRecordMap">
        <include refid="selectTradeRecordVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="TradeRecordMap">
        <include refid="selectTradeRecordVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

