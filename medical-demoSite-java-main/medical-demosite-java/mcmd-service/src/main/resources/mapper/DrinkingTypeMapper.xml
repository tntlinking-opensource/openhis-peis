<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.dao.DrinkingTypeMapper">

    <resultMap type="com.center.medical.bean.model.DrinkingType" id="DrinkingTypeMap">
        <id property="id" column="id"/>
        <result property="typeName" column="type_name"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectDrinkingTypeVo">
        select id, type_name, createdate, modifydate
        from md_drinking_type
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="DrinkingTypeMap">
        <include refid="selectDrinkingTypeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="DrinkingTypeMap">
        <include refid="selectDrinkingTypeVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

