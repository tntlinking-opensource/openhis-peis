<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.dao.PacsInspectChargeMapper">

    <resultMap type="com.center.medical.bean.model.PacsInspectCharge" id="PacsInspectChargeMap">
        <id property="id" column="id"/>
        <result property="chargeId" column="charge_id"/>
        <result property="inspectId" column="inspect_id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="isDelete" column="is_delete"/>
        <result property="orderIndex" column="order_index"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectPacsInspectChargeVo">
        select id, charge_id, inspect_id, createdate, modifydate, is_delete, order_index
        from md_pacs_inspect_charge
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="PacsInspectChargeMap">
        <include refid="selectPacsInspectChargeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="PacsInspectChargeMap">
        <include refid="selectPacsInspectChargeVo"/>
        <where>
            id = #{id}
        </where>
    </select>


    <!-- 得到相同检查项目的不同收费项目 -->
    <select id="getRepeatItems" resultType="string">
        SELECT
            ic.charge_id
        FROM
            md_pacs_inspect_charge ic
                INNER JOIN md_pacs_basexamltem b ON b.id = ic.inspect_id
        WHERE
            ic.inspect_id = #{inspectid}
          AND ic.charge_id IN (#{itemId})
          AND ic.is_delete = 0
          AND ( b.f_can_dup IS NULL OR b.f_can_dup != 1 )
        GROUP BY
            ic.charge_id
    </select>
</mapper>

