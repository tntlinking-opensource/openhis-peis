<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.sync.dao.SyncDataLogMapper">

    <resultMap type="com.center.medical.sync.bean.model.SyncDataLog" id="SyncDataLogMap">
        <id property="logId" column="log_id"/>
        <result property="dataId" column="data_id"/>
        <result property="dataType" column="data_type"/>
        <result property="optType" column="opt_type"/>
        <result property="version" column="version"/>
        <result property="branchId" column="branch_id"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSyncDataLogVo">
        select log_id,
               data_id,
               data_type,
               opt_type,
               version,
               branch_id,
               remark,
               status,
               createdate,
               modifydate
        from md_sync_data_log
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SyncDataLogMap">
        <include refid="selectSyncDataLogVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SyncDataLogMap">
        <include refid="selectSyncDataLogVo"/>
        <where>
            logId = #{id}
        </where>
    </select>

</mapper>

