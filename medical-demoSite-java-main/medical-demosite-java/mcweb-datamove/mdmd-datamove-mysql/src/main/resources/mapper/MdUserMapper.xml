<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdUserMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdUser" id="MdUserMap">
        <id property="userId" column="user_id"/>
        <result property="phone" column="phone"/>
        <result property="nickName" column="nick_name"/>
        <result property="realName" column="real_name"/>
        <result property="password" column="password"/>
        <result property="payPassword" column="pay_password"/>
        <result property="idCard" column="id_card"/>
        <result property="archiveId" column="archive_id"/>
        <result property="userMemo" column="user_memo"/>
        <result property="status" column="status"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdUserVo">
        select user_id,
               phone,
               nick_name,
               real_name,
               password,
               pay_password,
               id_card,
               archive_id,
               user_memo,
               status,
               createdate,
               modifydate
        from md_user
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdUserMap">
        <include refid="selectMdUserVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdUserMap">
        <include refid="selectMdUserVo"/>
        <where>
            userId = #{id}
        </where>
    </select>

</mapper>

