<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdDrinkingTypeMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdDrinkingType" id="MdDrinkingTypeMap">
        <id property="id" column="id"/>
        <result property="typeName" column="type_name"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdDrinkingTypeVo">
        select id, type_name, createdate, modifydate
        from md_drinking_type
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdDrinkingTypeMap">
        <include refid="selectMdDrinkingTypeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdDrinkingTypeMap">
        <include refid="selectMdDrinkingTypeVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

