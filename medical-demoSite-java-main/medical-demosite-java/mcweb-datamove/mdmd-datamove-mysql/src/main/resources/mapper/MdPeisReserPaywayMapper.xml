<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdPeisReserPaywayMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdPeisReserPayway" id="MdPeisReserPaywayMap">
        <id property="id" column="id"/>
        <result property="idCharge" column="id_charge"/>
        <result property="moneyamountpaid" column="moneyamountpaid"/>
        <result property="moneyamountpaiddate" column="moneyamountpaiddate"/>
        <result property="idFeecharger" column="id_feecharger"/>
        <result property="idPayway" column="id_payway"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="note" column="note"/>
        <result property="cardno" column="cardno"/>
        <result property="isCharged" column="is_charged"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdPeisReserPaywayVo">
        select id,
               id_charge,
               moneyamountpaid,
               moneyamountpaiddate,
               id_feecharger,
               id_payway,
               createdate,
               modifydate,
               note,
               cardno,
               is_charged
        from md_peis_reser_payway
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdPeisReserPaywayMap">
        <include refid="selectMdPeisReserPaywayVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdPeisReserPaywayMap">
        <include refid="selectMdPeisReserPaywayVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

