<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdNotificationmethodMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdNotificationmethod" id="MdNotificationmethodMap">
        <id property="id" column="id"/>
        <result property="methodName" column="method_name"/>
        <result property="inputCode" column="input_code"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="creater" column="creater"/>
        <result property="isDelete" column="is_delete"/>
        <result property="isSendNotice" column="is_send_notice"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdNotificationmethodVo">
        select id,
               method_name,
               input_code,
               createdate,
               modifydate,
               creater,
               is_delete,
               is_send_notice
        from md_notificationmethod
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdNotificationmethodMap">
        <include refid="selectMdNotificationmethodVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdNotificationmethodMap">
        <include refid="selectMdNotificationmethodVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

