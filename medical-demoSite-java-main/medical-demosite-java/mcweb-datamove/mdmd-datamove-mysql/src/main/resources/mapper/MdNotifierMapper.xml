<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdNotifierMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdNotifier" id="MdNotifierMap">
        <id property="id" column="id"/>
        <result property="reportId" column="report_id"/>
        <result property="notifierId" column="notifier_id"/>
        <result property="notificationMode" column="notification_mode"/>
        <result property="notificationDetails" column="notification_details"/>
        <result property="notificationTime" column="notification_time"/>
        <result property="notificationResult" column="notification_result"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="memo" column="memo"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdNotifierVo">
        select id,
               report_id,
               notifier_id,
               notification_mode,
               notification_details,
               notification_time,
               notification_result,
               createdate,
               modifydate,
               memo
        from md_notifier
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdNotifierMap">
        <include refid="selectMdNotifierVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdNotifierMap">
        <include refid="selectMdNotifierVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

