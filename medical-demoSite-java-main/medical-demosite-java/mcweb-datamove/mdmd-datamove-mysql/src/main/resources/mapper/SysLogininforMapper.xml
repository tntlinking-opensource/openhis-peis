<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.SysLogininforMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.SysLogininfor" id="SysLogininforMap">
        <id property="infoId" column="info_id"/>
        <result property="userName" column="user_name"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="status" column="status"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSysLogininforVo">
        select info_id,
               user_name,
               ipaddr,
               login_location,
               browser,
               os,
               status,
               msg,
               login_time
        from sys_logininfor
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SysLogininforMap">
        <include refid="selectSysLogininforVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SysLogininforMap">
        <include refid="selectSysLogininforVo"/>
        <where>
            infoId = #{id}
        </where>
    </select>

</mapper>

