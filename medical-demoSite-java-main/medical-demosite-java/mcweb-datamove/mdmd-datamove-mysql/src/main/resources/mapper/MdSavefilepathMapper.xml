<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdSavefilepathMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdSavefilepath" id="MdSavefilepathMap">
        <id property="id" column="id"/>
        <result property="ggid" column="ggid"/>
        <result property="filepath" column="filepath"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdSavefilepathVo">
        select id, ggid, filepath, createdate, modifydate
        from md_savefilepath
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdSavefilepathMap">
        <include refid="selectMdSavefilepathVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdSavefilepathMap">
        <include refid="selectMdSavefilepathVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

