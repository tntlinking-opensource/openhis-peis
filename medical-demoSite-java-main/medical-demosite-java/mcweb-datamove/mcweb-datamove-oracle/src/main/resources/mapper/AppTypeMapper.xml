<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.AppTypeMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.AppType" id="AppTypeMap">
        <id property="id" column="ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
        <result property="typeName" column="TYPE_NAME"/>
        <result property="inputCode" column="INPUT_CODE"/>
        <result property="typeSort" column="TYPE_SORT"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectAppTypeVo">
        select ID, CREATEDATE, MODIFYDATE, TYPE_NAME, INPUT_CODE, TYPE_SORT
        from APP_TYPE
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="AppTypeMap">
        <include refid="selectAppTypeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="AppTypeMap">
        <include refid="selectAppTypeVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

