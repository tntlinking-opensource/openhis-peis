<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.report.dao.PacsResultThirdMapper">
    <select id="getQrCodeContentByPatientcode" resultType="java.lang.String">
        SELECT
            DISTINCT qr_code_content
        FROM
            md_pacs_result_third
        WHERE
            patient_id IN (
                #{patientcode},
                #{patientcode8}
            )
            AND qr_code_content IS NOT NULL
            AND qr_code_content != ''
    </select>
</mapper>
