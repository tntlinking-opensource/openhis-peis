<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.report.dao.ReportConfigMapper">

    <resultMap type="com.center.medical.report.bean.model.ReportConfig" id="ReportConfigMap">
        <id property="id" column="id"/>
        <result property="branchId" column="branch_id"/>
        <result property="status" column="status"/>
        <result property="isDefault" column="is_default"/>
        <result property="content" column="content"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="memo" column="memo"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectReportConfigVo">
        select id,
               branch_id,
               status,
               is_default,
               content,
               createdate,
               modifydate,
               memo
        from md_report_config
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultType="com.center.medical.report.bean.model.ReportConfig">
        select mrc.id,
               mrc.branch_id,
               mrc.status,
               mrc.is_default,
               mrc.content,
               mrc.createdate,
               mrc.modifydate,
               mrc.memo,
               sb.fzx branchName
        from md_report_config mrc
        left join sys_branch sb on sb.branch_id = mrc.branch_id
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="ReportConfigMap">
        <include refid="selectReportConfigVo"/>
        <where>
            id = #{id}
        </where>
    </select>


    <!-- 获取分中心报告配置 -->
    <select id="getBranchConfig" resultType="java.lang.String">
        select content
        from md_report_config
        <where>
            status = 0
            AND branch_id = #{branchId}
        </where>
    </select>
</mapper>

