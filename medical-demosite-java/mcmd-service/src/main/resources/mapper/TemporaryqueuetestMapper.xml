<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.dao.TemporaryqueuetestMapper">

    <resultMap type="com.center.medical.bean.model.Temporaryqueuetest" id="TemporaryqueuetestMap">
        <id property="id" column="id"/>
        <result property="patientcode" column="patientcode"/>
        <result property="transmitting" column="transmitting"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectTemporaryqueuetestVo">
        select id, patientcode, transmitting
        from md_temporaryqueuetest
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="TemporaryqueuetestMap">
        <include refid="selectTemporaryqueuetestVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="TemporaryqueuetestMap">
        <include refid="selectTemporaryqueuetestVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

