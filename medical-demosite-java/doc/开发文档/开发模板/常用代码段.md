## 常用代码段

## 获取登录用户

LoginUser loginUser = SecurityUtils.getLoginUser();

## 获取登录用户的用户编号（md开头的数据表，userid都是用UserNo！！）

String userNo = SecurityUtils.getUserNo();

## 获取登录用户的分中心ID（md开头的数据表，分中心id都是用sys_branch表中的branch_id！！）

String cid = SecurityUtils.getCid();

## 对象与对象之间的转化，快速赋值

DwHarm fh = mapperFacade.map(zyHarmClass, DwHarm.class);

## 拷贝

BeanUtils.copyProperties(clientCommon, sellCustomer);

## 抛异常

throw new ServiceException("获取用户ID异常");

## 雪花算法

private final Snowflake snowflake; String.valueOf(snowflake.nextId())

## xml文件

<if test="param.inputCode!=null and param.inputCode!=''">
    AND input_code like concat('%',#{param.inputCode},'%')
</if>

## 实体类，当前属性不是数据库的字段

@TableField(exist = false)
@ApiModelProperty(name = "categoryName", value = "产品类目名称", dataType = "String")
@ApiParam(hidden = true)
private String categoryName;

## 文件上传路径，路径地址在老系统下的doc_config.properties文件中，新系统在FileTypePath中

String url = FileTypePath.NET_URL

## 获取汉字的拼音首字母放入输入码

basconclusion.setInputCode(ToolUtil .getHanziPinyinHeadChar(basconclusion.getName()));

## 查询有没有数据管理的权限

Boolean frole = SecurityUtils.hasRole(RoleAuthName.DATA_MANAGE);

## @see 枚举类型链接地址

```java
/**
 * @see enums.com.center.medical.bean.JinanStatus
 */

```

```java
@ApiParam(hidden = true)
@ApiModelProperty(value = "是否拥有决策管理者角色")
private Boolean jueceManage;
```

```java
return R.ok("功能尚未完善，暂时无法用！");
```