<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.reservation.dao.ReservationDefaultMapper">

    <resultMap type="com.center.medical.reservation.bean.model.ReservationDefault" id="ReservationDefaultMap">
        <id property="id" column="id"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="maxNum" column="max_num"/>
        <result property="levelId" column="level_id"/>
        <result property="remark" column="remark"/>
        <result property="branchId" column="branch_id"/>
        <result property="openDay" column="open_day"/>
        <result property="amOrPm" column="am_or_pm"/>
        <result property="status" column="status"/>
        <result property="createdate" column="createdate"/>
        <result property="creator" column="creator"/>
        <result property="modifydate" column="modifydate"/>
        <result property="modifier" column="modifier"/>
        <result property="branchName" column="branch_name"/>
        <result property="levelName" column="level_name"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectReservationDefaultVo">
        SELECT rd.id,
               rd.start_time,
               rd.end_time,
               rd.max_num,
               rd.level_id,
               rd.remark,
               rd.branch_id,
               rd.open_day,
               rd.am_or_pm,
               rd.`status`,
               rd.createdate,
               rd.creator,
               rd.modifydate,
               rd.modifier,
               b.fzx AS branch_name,
               ul.level_name
        FROM md_reservation_default rd
                 LEFT JOIN md_user_level ul ON rd.level_id = ul.level_id
                 LEFT JOIN sys_branch b ON rd.branch_id = b.branch_id
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="ReservationDefaultMap">
        <include refid="selectReservationDefaultVo"/>
        <where>
            <if test="param.branchId!=null and param.branchId!=''">
                AND rd.branch_id = #{param.branchId}
            </if>
            <if test="param.levelId!=null">
                AND rd.level_id = #{param.levelId}
            </if>
            <if test="param.status!=null">
                AND rd.status = #{param.status}
            </if>
        </where>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="ReservationDefaultMap">
        <include refid="selectReservationDefaultVo"/>
        <where>
            rd.id = #{id}
        </where>
    </select>

</mapper>

