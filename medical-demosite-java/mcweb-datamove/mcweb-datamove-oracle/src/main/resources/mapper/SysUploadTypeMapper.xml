<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.SysUploadTypeMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.SysUploadType" id="SysUploadTypeMap">
        <id property="id" column="ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
        <result property="uploadType" column="UPLOAD_TYPE"/>
        <result property="uploadName" column="UPLOAD_NAME"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSysUploadTypeVo">
        select ID, CREATEDATE, MODIFYDATE, UPLOAD_TYPE, UPLOAD_NAME
        from SYS_UPLOAD_TYPE
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SysUploadTypeMap">
        <include refid="selectSysUploadTypeVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SysUploadTypeMap">
        <include refid="selectSysUploadTypeVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

