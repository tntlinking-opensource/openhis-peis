<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.AppLabelMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.AppLabel" id="AppLabelMap">
        <id property="id" column="ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
        <result property="label" column="LABEL"/>
        <result property="isDelete" column="IS_DELETE"/>
        <result property="idx" column="IDX"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectAppLabelVo">
        select ID, CREATEDATE, MODIFYDATE, LABEL, IS_DELETE, IDX
        from APP_LABEL
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="AppLabelMap">
        <include refid="selectAppLabelVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="AppLabelMap">
        <include refid="selectAppLabelVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

