<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.QxUseDepMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.QxUseDep" id="QxUseDepMap">
        <id property="id" column="ID"/>
        <result property="depId" column="DEP_ID"/>
        <result property="userId" column="USER_ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectQxUseDepVo">
        select ID, DEP_ID, USER_ID, CREATEDATE, MODIFYDATE
        from QX_USE_DEP
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="QxUseDepMap">
        <include refid="selectQxUseDepVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="QxUseDepMap">
        <include refid="selectQxUseDepVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

