<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.DiningRecordMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.DiningRecord" id="DiningRecordMap">
        <id property="id" column="ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
        <result property="patientcode" column="PATIENTCODE"/>
        <result property="diningTime" column="DINING_TIME"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectDiningRecordVo">
        select ID, CREATEDATE, MODIFYDATE, PATIENTCODE, DINING_TIME
        from DINING_RECORD
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="DiningRecordMap">
        <include refid="selectDiningRecordVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="DiningRecordMap">
        <include refid="selectDiningRecordVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

