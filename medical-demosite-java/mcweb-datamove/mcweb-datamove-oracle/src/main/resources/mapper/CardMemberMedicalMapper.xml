<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.oracle.dao.CardMemberMedicalMapper">

    <resultMap type="com.center.medical.datamove.oracle.bean.model.CardMemberMedical" id="CardMemberMedicalMap">
        <id property="id" column="ID"/>
        <result property="createdate" column="CREATEDATE"/>
        <result property="modifydate" column="MODIFYDATE"/>
        <result property="memberId" column="MEMBER_ID"/>
        <result property="medicalId" column="MEDICAL_ID"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectCardMemberMedicalVo">
        select ID, CREATEDATE, MODIFYDATE, MEMBER_ID, MEDICAL_ID
        from CARD_MEMBER_MEDICAL
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="CardMemberMedicalMap">
        <include refid="selectCardMemberMedicalVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="CardMemberMedicalMap">
        <include refid="selectCardMemberMedicalVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

