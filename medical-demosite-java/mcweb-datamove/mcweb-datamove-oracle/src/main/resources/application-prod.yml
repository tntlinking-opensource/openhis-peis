# 正式环境配置

# 项目相关配置
zhongkang:
  # 名称
  name: 沃德健管云平台
  # 版本
  version: V2.6.1
  # 版权年份
  copyrightYear: 2022
  # 实例演示开关
  demoEnabled: true
  # 文件路径 示例（ Windows配置D:/zhongkang/files/uploadPath，Linux配置 /newimage）
  profile: /www/nas
  profileLinux: /www/nas
  # 获取ip地址开关
  addressEnabled: false
  # 验证码类型 math 数组计算 char 字符验证
  captchaType: math
  #是否线上系统
  online: false
  #分中心ID
  fzxId:
  #分中心简码
  fzxjm:

server:
  port: 8082
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    druid:
      # 👇 开启回收机制
      removeAbandoned: true
      # 👇 超时时间（单位秒）：事务/连接超过 5 分钟未关闭就回收
      removeAbandonedTimeout: 50
      # 👇 开启日志，排查泄露源
      logAbandoned: true
      # 主库数据源
      master:
        url: jdbc:mysql://XXX.XXX.XXX.XXX:3306/XXX?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&tinyInt1isBit=false&transformedBitIsBoolean=false
        username: XXX
        password: XXX
      # 从库数据源
      slave:
        # 从数据源开关/默认关闭
        enabled: false
        url:
        username:
        password:
      # 初始连接数
      initialSize: 5
      # 最小连接池数量
      minIdle: 10
      # 最大连接池数量
      maxActive: 20
      # 配置获取连接等待超时的时间
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      # 配置一个连接在池中最大生存的时间，单位是毫秒
      maxEvictableIdleTimeMillis: 900000
      # 配置检测连接是否有效
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      webStatFilter:
        enabled: true
      statViewServlet:
        enabled: true
        # 设置白名单，不填则允许所有访问
        allow:
        url-pattern: /druid/*
        # 控制台管理用户名和密码
        login-username: world
        login-password: 123456
      filter:
        stat:
          enabled: true
          # 慢SQL记录
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
  redis:
    redisson:
      config: classpath:redisson/redisson-prod.yml
logging:
  config: classpath:logback/logback-prod.xml
#elastic:
#  address: http://localhost:9200
