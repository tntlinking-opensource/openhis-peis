<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdAttachFileMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdAttachFile" id="MdAttachFileMap">
        <id property="fileId" column="file_id"/>
        <result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/>
        <result property="fileName" column="file_name"/>
        <result property="fileSize" column="file_size"/>
        <result property="uploadTime" column="upload_time"/>
        <result property="branchId" column="branch_id"/>
        <result property="type" column="type"/>
        <result property="attachFileGroupId" column="attach_file_group_id"/>
        <result property="status" column="status"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdAttachFileVo">
        select file_id,
               file_path,
               file_type,
               file_name,
               file_size,
               upload_time,
               branch_id,
               type,
               attach_file_group_id,
               status
        from md_attach_file
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdAttachFileMap">
        <include refid="selectMdAttachFileVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdAttachFileMap">
        <include refid="selectMdAttachFileVo"/>
        <where>
            fileId = #{id}
        </where>
    </select>

</mapper>

