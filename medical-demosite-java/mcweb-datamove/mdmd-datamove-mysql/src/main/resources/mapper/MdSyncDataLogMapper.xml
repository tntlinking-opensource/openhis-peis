<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdSyncDataLogMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdSyncDataLog" id="MdSyncDataLogMap">
        <id property="logId" column="log_id"/>
        <result property="dataId" column="data_id"/>
        <result property="dataType" column="data_type"/>
        <result property="optType" column="opt_type"/>
        <result property="version" column="version"/>
        <result property="branchId" column="branch_id"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdSyncDataLogVo">
        select log_id,
               data_id,
               data_type,
               opt_type,
               version,
               branch_id,
               remark,
               status,
               createdate,
               modifydate
        from md_sync_data_log
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdSyncDataLogMap">
        <include refid="selectMdSyncDataLogVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdSyncDataLogMap">
        <include refid="selectMdSyncDataLogVo"/>
        <where>
            logId = #{id}
        </where>
    </select>

</mapper>

