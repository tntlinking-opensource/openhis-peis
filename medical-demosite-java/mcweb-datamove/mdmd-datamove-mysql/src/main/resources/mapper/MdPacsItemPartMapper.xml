<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdPacsItemPartMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdPacsItemPart" id="MdPacsItemPartMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="itemId" column="item_id"/>
        <result property="partId" column="part_id"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdPacsItemPartVo">
        select id, createdate, modifydate, item_id, part_id
        from md_pacs_item_part
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdPacsItemPartMap">
        <include refid="selectMdPacsItemPartVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdPacsItemPartMap">
        <include refid="selectMdPacsItemPartVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

