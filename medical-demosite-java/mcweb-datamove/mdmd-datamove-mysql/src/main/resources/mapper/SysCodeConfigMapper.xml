<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.SysCodeConfigMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.SysCodeConfig" id="SysCodeConfigMap">
        <id property="id" column="id"/>
        <result property="keyText" column="key_text"/>
        <result property="valueText" column="value_text"/>
        <result property="bsFlag" column="bs_flag"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSysCodeConfigVo">
        select id,
               key_text,
               value_text,
               bs_flag,
               remark,
               status,
               createdate,
               modifydate
        from sys_code_config
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SysCodeConfigMap">
        <include refid="selectSysCodeConfigVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SysCodeConfigMap">
        <include refid="selectSysCodeConfigVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

