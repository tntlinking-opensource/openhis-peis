<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdReviewProjectMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdReviewProject" id="MdReviewProjectMap">
        <id property="id" column="id"/>
        <result property="itemsId" column="items_id"/>
        <result property="itemsName" column="items_name"/>
        <result property="reviewId" column="review_id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdReviewProjectVo">
        select id, items_id, items_name, review_id, createdate, modifydate
        from md_review_project
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdReviewProjectMap">
        <include refid="selectMdReviewProjectVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdReviewProjectMap">
        <include refid="selectMdReviewProjectVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

