<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdPatientUsersMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdPatientUsers" id="MdPatientUsersMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="idCard" column="id_card"/>
        <result property="archiveId" column="archive_id"/>
        <result property="status" column="status"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdPatientUsersVo">
        select id,
               createdate,
               modifydate,
               phone,
               password,
               name,
               id_card,
               archive_id,
               status
        from md_patient_users
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdPatientUsersMap">
        <include refid="selectMdPatientUsersVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdPatientUsersMap">
        <include refid="selectMdPatientUsersVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

