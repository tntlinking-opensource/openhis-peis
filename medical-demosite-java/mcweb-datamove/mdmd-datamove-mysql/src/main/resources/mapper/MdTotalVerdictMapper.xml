<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdTotalVerdictMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdTotalVerdict" id="MdTotalVerdictMap">
        <id property="id" column="id"/>
        <result property="basconclusionId" column="basconclusion_id"/>
        <result property="totalId" column="total_id"/>
        <result property="divisionId" column="division_id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="isDelete" column="is_delete"/>
        <result property="diseaseHealth" column="disease_health"/>
        <result property="flag" column="flag"/>
        <result property="totalAdvice" column="total_advice"/>
        <result property="basconclusionName" column="basconclusion_name"/>
        <result property="mergeId" column="merge_id"/>
        <result property="mergeName" column="merge_name"/>
        <result property="verdictSort" column="verdict_sort"/>
        <result property="suggestiongroup" column="suggestiongroup"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdTotalVerdictVo">
        select id,
               basconclusion_id,
               total_id,
               division_id,
               createdate,
               modifydate,
               is_delete,
               disease_health,
               flag,
               total_advice,
               basconclusion_name,
               merge_id,
               merge_name,
               verdict_sort,
               suggestiongroup
        from md_total_verdict
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdTotalVerdictMap">
        <include refid="selectMdTotalVerdictVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdTotalVerdictMap">
        <include refid="selectMdTotalVerdictVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

