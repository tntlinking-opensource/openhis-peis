<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.BaseDictionaryMatchMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.BaseDictionaryMatch" id="BaseDictionaryMatchMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="dictionaryId" column="dictionary_id"/>
        <result property="medicalId" column="medical_id"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectBaseDictionaryMatchVo">
        select id, createdate, modifydate, dictionary_id, medical_id
        from base_dictionary_match
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="BaseDictionaryMatchMap">
        <include refid="selectBaseDictionaryMatchVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="BaseDictionaryMatchMap">
        <include refid="selectBaseDictionaryMatchVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

