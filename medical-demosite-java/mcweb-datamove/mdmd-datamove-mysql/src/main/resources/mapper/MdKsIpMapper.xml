<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datamove.common.dao.MdKsIpMapper">

    <resultMap type="com.center.medical.datamove.common.bean.model.MdKsIp" id="MdKsIpMap">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="ip" column="ip"/>
        <result property="ksId" column="ks_id"/>
        <result property="remark" column="remark"/>
        <result property="branchId" column="branch_id"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectMdKsIpVo">
        select id,
               name,
               ip,
               ks_id,
               remark,
               branch_id,
               status,
               create_time,
               update_time
        from md_ks_ip
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="MdKsIpMap">
        <include refid="selectMdKsIpVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="MdKsIpMap">
        <include refid="selectMdKsIpVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

