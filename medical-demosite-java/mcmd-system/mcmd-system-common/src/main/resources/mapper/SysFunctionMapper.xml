<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.system.dao.SysFunctionMapper">

    <resultMap type="com.center.medical.system.bean.model.SysFunction" id="SysFunctionMap">
        <id property="functionId" column="function_id"/>
        <result property="functionName" column="function_name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectSysFunctionVo">
        select function_id, function_name, status, remark, create_time, update_time
        from sys_function
    </sql>

    <!-- 查询列表数据 -->
    <select id="getPage" resultMap="SysFunctionMap">
        <include refid="selectSysFunctionVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="SysFunctionMap">
        <include refid="selectSysFunctionVo"/>
        <where>
            functionId = #{id}
        </where>
    </select>

</mapper>

