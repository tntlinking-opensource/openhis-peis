<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.system.dao.UserHarmClassMapper">

    <resultMap type="com.center.medical.system.bean.model.UserHarmClass" id="UserHarmClassMap">
        <id property="id" column="id"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
        <result property="userId" column="user_id"/>
        <result property="classId" column="class_id"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectUserHarmClassVo">
        select id, createdate, modifydate, user_id, class_id
        from md_user_harm_class
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="UserHarmClassMap">
        <include refid="selectUserHarmClassVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="UserHarmClassMap">
        <include refid="selectUserHarmClassVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

