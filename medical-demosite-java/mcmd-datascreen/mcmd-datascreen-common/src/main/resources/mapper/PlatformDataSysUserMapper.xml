<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.datascreen.dao.PlatformDataSysUserMapper">
    <select id="selectUserCountByRoleKey" resultType="int">
        SELECT
            count(*)
        FROM
            sys_user u
        WHERE
            u.del_flag = '0'
        AND u. STATUS = '0'
        AND EXISTS (
            SELECT
                1
            FROM
                sys_user_role sur
            INNER JOIN sys_role r ON r.role_id = sur.role_id
            WHERE
                sur.user_id = u.user_id
            AND r.del_flag = '0'
            AND r. STATUS = '0'
            AND r.role_key = #{roleKey}
        )
    </select>
</mapper>