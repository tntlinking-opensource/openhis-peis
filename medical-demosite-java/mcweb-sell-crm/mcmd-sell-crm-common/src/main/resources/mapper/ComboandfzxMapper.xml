<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.center.medical.sellcrm.dao.ComboandfzxMapper">

    <resultMap type="com.center.medical.sellcrm.bean.model.Comboandfzx" id="ComboandfzxMap">
        <id property="id" column="id"/>
        <result property="tcid" column="tcid"/>
        <result property="fzxid" column="fzxid"/>
        <result property="tbzt" column="tbzt"/>
        <result property="createdate" column="createdate"/>
        <result property="modifydate" column="modifydate"/>
    </resultMap>

    <!-- 数据表字段属性 -->
    <sql id="selectComboandfzxVo">
        select id, tcid, fzxid, tbzt, createdate, modifydate
        from md_comboandfzx
    </sql>

    <!-- 查询列表数据 -->
    <select id="getList" resultMap="ComboandfzxMap">
        <include refid="selectComboandfzxVo"/>
    </select>

    <!-- 根据主键id获取记录详情 -->
    <select id="getInfoById" resultMap="ComboandfzxMap">
        <include refid="selectComboandfzxVo"/>
        <where>
            id = #{id}
        </where>
    </select>

</mapper>

